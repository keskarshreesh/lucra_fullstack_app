export type SignupDetails = {
    firstname: string;
    lastname: string;
    username: string;
    email: string;
    password: string;
};

export type LoginDetails = {
    username: string;
    password: string;
};